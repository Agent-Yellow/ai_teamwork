# Operational Preferences

- Preferred Frameworks: Vercel, Tailwind, plain HTML/JS where possible for simplicity.
- Task Loop: Always scaffold the repo first before deploying.
- Safe retries: If a deterministic bash script fails with a network error, retry up to 3 times before moving job to failed.
